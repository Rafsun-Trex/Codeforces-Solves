#include<stdio.h>
int main()
{
    int n,i,count=0;
    char arr[60];
    scanf("%d",&n);
    scanf("%s",&arr);
    for(i=0; i<n-1; i++)
    {
        if(arr[i]==arr[i+1])
        {
            count++;
        }
    }
    printf("%d\n",count);
}
