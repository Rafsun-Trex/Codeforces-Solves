#include<stdio.h>
int main()
{
    int n, i, x[100], a[150];
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&x[i]);
    }
    for(i=0;i<n;i++)
    {
        a[x[i]]=i+1;
    }
    for(i=1;i<=n;i++)
    {
        printf("%d ",a[i]);
    }
}
