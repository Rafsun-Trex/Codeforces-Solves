#include<stdio.h>
int main()
{
    int n,i,x1,x2,x3,sum=0;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d %d %d",&x1,&x2,&x3);
        if(x1+x2+x3>=2)
        {
            sum+=1;
        }
    }
    printf("%d\n",sum);
}
