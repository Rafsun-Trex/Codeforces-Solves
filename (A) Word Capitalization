#include<stdio.h>
#include<string.h>
int main()
{
    char ch[1010];
    int i, l;
    gets(ch);
    l = strlen(ch);
    if(ch[0]>=97&&ch[0]<=122)
    {
        ch[0] = ch[0] - 32;
    }
    else
    {
        ch[0] = ch[0];
    }
    printf("%s\n",ch);
}
