#include<stdio.h>
int main()
{
    int n, i, sum = 0, count = 0;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        sum += i*(i+1)/2;
        if(n>=sum)
        {
            count++;
        }
        else
        {
            break;
        }
    }
    printf("%d\n",count);
    return 0;
}
