#include<stdio.h>
#include<string.h>
int main()
{
    char str[100];
    int i,l,count=0;
    gets(str);
    l = strlen(str);
    for(i=0;i<l;i++)
    {
        if(str[i]=='H'||str[i]=='Q'||str[i]=='9')
        {
            count++;
        }
    }
    if(count>=1)
    {
        printf("YES\n");
    }
    else
    {
        printf("NO\n");
    }
}
